type User {
  email: String!
  firstName: String!
  lastName: String!
  dateOfBirth: String
  city: String
  country: String
  profession: String
  maritalStatus: String
}

type Journal {
  id: ID!
  topic: String!
  body: String!
  fontSettings: FontSettings
  images: [JournalImage!]!
  createdAt: String!
  updatedAt: String!
}

type JournalImage {
  id: ID!
  url: String!
  sortOrder: Int!
}

type FontSettings {
  heading: String
  subheading: String
  body: String
  mono: String
}

input JournalInput {
  topic: String!
  body: String!
  fontHeading: String
  fontSubheading: String
  fontBody: String
  fontMono: String
  imageUrls: [String!]
}

type Query {
  getUser(email: String!): User
  getJournals(sortBy: SortOrder): [Journal!]!
  getJournal(id: ID!): Journal
  me: User
}

enum SortOrder {
  NEWEST_FIRST
  OLDEST_FIRST
  ALPHABETICAL
}

type Mutation {
  signup(
    firstName: String!
    lastName: String!
    email: String!
    dateOfBirth: String!
    city: String!
    country: String!
    profession: String!
    maritalStatus: String!
  ): String

  login(email: String!): String
  verifyOTP(email: String!, otp: String!): AuthPayload
  
  # Profile mutations
  updateUser(input: UpdateUserInput!): User!
  requestEmailChange(newEmail: String!): String!
  confirmEmailChange(newEmail: String!, otp: String!): User!
  
  # Journal mutations  
  createJournal(input: JournalInput!): Journal!
  updateJournal(id: ID!, input: JournalInput!): Journal!
  deleteJournal(id: ID!): Boolean!
}

input UpdateUserInput {
  firstName: String
  lastName: String
  dateOfBirth: String
  city: String
  country: String
  profession: String
  maritalStatus: String
}

type AuthPayload {
  token: String
  user: User
}
